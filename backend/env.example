# This file is superseded by .env.example (note the dot prefix).
# Please use backend/.env.example as the canonical environment variable reference.
# Copy it to backend/.env and fill in your real values.
#
# See .env.example for all required variables.
